pipeline {
    agent any 
    stages{
        stage("Build"){
            steps{
                script{
                    def course = "k8s"
                    if ("course == docker") 
                     println ("Thanks for enrolling course")
                     else 
                     println ("Do enroll")
                    
                }
            }
        }
    }
}
